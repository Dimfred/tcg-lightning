# 0.1.36

## Features

- Plain decklist import now shows proper preview instead of raw line count
- DeepSeek and ChatGPT providers, including thinking flag support
- Partner commander support
- Hover-out animation for card previews
- Proxy dialog supports adjustable crop mark size

## Fixes

- Import/export controls sizing fixed (import tags, import, export, command palette)
- Double-faced card hover fixed for right-edge overflow (Aang, Swift Savior // ...)
- macOS-aware scrolling behavior fixed (`Cmd` + scroll)
- `Cmd+O` deck loading infinite loop fixed
- Collection dedup bug fixed so Commander's Sphere and other cards show correctly
- Collection upload watcher now handles any count via `(\d+)` pattern
- Deckcard not found error fixed
- Provider deletion now refreshes provider list immediately
- Partner commander color identity validation fixed
- Biome issue fixed in `DeckSizeValidation.svelte`

## Refactors

- Proxy dialog refactor
- Bug report submit flow refactor (feature-local service)
